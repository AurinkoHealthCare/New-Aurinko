import React from "react";

const Gallerys  = () => {
    return (
        <div>
            <h1>This is the Gallery page in which we can edit, change and delete in Gallery page</h1>
        </div>
    );
};

export default Gallerys ;
