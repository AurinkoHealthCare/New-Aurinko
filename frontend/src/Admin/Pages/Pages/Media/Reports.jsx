import React from "react";

const Report = () => {
    return (
        <div>
            <h1>This is the Report page in which we can edit, change and delete in Report page</h1>
        </div>
    );
};

export default Report;
