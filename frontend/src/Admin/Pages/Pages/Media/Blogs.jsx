import React from "react";

const Blogs = () => {
    return (
        <div>
            <h1>This is the Blog page in which we can edit, change and delete in Blog page</h1>
        </div>
    );
};

export default Blogs;
