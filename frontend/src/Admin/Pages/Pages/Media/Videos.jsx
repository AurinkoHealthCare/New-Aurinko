import React from "react";

const Video = () => {
    return (
        <div>
            <h1>This is the video page in which we can edit, change and delete in video page</h1>
        </div>
    );
};

export default Video;
