import React from "react";

const ImageSlider = () => {
    return (
        <div>
            <h1>This is the Image slider page in which we can edit, change and delete in Image slider page</h1>
        </div>
    );
};

export default ImageSlider;