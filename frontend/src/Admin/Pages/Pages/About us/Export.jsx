import React from "react";

const Exporte = () => {
    return (
        <div>
            <h1>This is the export page of about us in which we can edit, change and delete in export page</h1>
        </div>
    );
};

export default Exporte;