import React from "react";

const Feedback = () => {
    return (
        <div>
            In this page we want to show all the forms submitted by the users
        </div>
    );
};  

export default Feedback;